package com.torneo.api.enums;

/**
 * Este enum define los tipos de usuario que existen en el sistema.
 * Cada usuario tendrá uno de estos roles, que define qué permisos tiene.
 */
public enum Role {
    ADMIN,
    ORGANIZADOR,
    JUGADOR
}
